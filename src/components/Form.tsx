import React from "react";

export default function Form () {
    return (
        <>
            <input type="text" placeholder="input search word"/>
            <button>search</button>
        </>
    );
}